name: Deploy Laravel to Hpanel

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Hpanel

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SERVER_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd /home/u376682195/domains/sideospary.com/new_artiknesia
            git clone https://github.com/ahmakbardev/seniman_ARTIKNESIA.git
            chmod +x ~/bin/composer
            echo "export PATH=$HOME/bin:$PATH" >> ~/.bash_profile
            source ~/.bash_profile
            which composer
            composer --version
            cd /home/u376682195/domains/sideospary.com/new_artiknesia/seniman_ARTIKNESIA/
            cp .env.example .env
            git pull origin develop
            composer install
            composer update
            php artisan route:clear
            php artisan config:cache
            php artisan optimize
            php artisan key:generate
            ln -s /home/u376682195/domains/sideospary.com/new_artiknesia/seniman_ARTIKNESIA/public /home/u376682195/domains/sideospary.com/public_html/seniman
